#include "main.h"
#include <stdio.h>

/**
 * strcpy - this function copies the string pointed to
 * by src 8ncluding the terminating null byte to the buffer
 * pointed to by dest
 *
 * @a: pointer to char
 * @n: number of elements to be printed
 *
 * Return: char
 */

char *_strcpy(char *dest, char *src)
{
	int i = 0;

	for (i = 0; i  
